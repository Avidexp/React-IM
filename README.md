# React-IM
Instant messaging Component using react, and socket.io


TO RUN LOCALLY:

  CD into chat_api
    - npm install
      -npm run compile
      -npm start
      
     Once Chat_Api is running
     
     -CD into react-im
        -npm install
         -npm start
